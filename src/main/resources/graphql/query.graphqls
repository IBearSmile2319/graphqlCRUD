type Query {
    findAllBrands: [Brand]
    findBrandById(id: ID!): Brand
    # models
    findAllModels: [Model]
    findModelById(id: ID!): Model
#    security
#    GenerateToken: String
    totpGen: String
}